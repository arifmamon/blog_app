import React, { Component } from 'react'
import { Text, View } from 'react-native'

const HomeScreen = () => {
    return (
        <view>

        </view>
    );
};
export default HomeScreen;